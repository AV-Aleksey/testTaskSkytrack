import {
  LOADING,
} from '../Actions/action-types'


const initialState = {

}

export function commonReducer(state = initialState, action) {
  switch (action.type) {

    default:
      return state
  }
}
